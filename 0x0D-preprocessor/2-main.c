#include <stdio.h>
/**
 * main - function uses predefined directive
 * __FILE__  to print file name it was
 * compiled from followed by new line
 *
 * Return: always 0
 */

int main(void)
{
printf("%s\n", __FILE__);

return (0);
}
